<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/choose_materi">

    <ImageButton
        android:id="@+id/backBarang"
        android:layout_width="70dp"
        android:layout_height="60dp"
        android:layout_marginTop="25dp"
        android:contentDescription="Kembali ke halaman sebelumnya (home)"
        android:background="@drawable/back_button"/>

    <!-- Tombol Kiri -->
    <ImageButton
        android:id="@+id/balik_barang"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@null"
        android:src="@drawable/leftbutton" />


    <GridLayout
        android:id="@+id/gridBarang"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_toStartOf="@id/lanjut_barang"
        android:layout_toEndOf="@id/balik_barang"
        android:columnCount="4"
        android:rowCount="2"
        android:padding="16dp"/>

    <!-- Tombol Kanan -->
    <ImageButton
        android:id="@+id/lanjut_barang"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@null"
        android:src="@drawable/right_button" />
</RelativeLayout>
